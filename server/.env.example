# MongoDB Atlas connection URI — replace with your real credentials
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/streak-it?retryWrites=true&w=majority

# Express server port
PORT=5000

# React dev server origin for CORS (update for production)
CLIENT_ORIGIN=http://localhost:5173